#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer
 * @a: parameter function
 * Return: -a or a;
 */
int _abs(int a)
{
	if (a < 0)
		return (-a);
	else
		return (a);
}

