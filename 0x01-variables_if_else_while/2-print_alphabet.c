#include <stdio.h>
/**
 * main - Entry point
 * Description - A program that prints the alphabet in lowercase
 * Return: 0 (success)
 */
int main(void)
{ 
	char a;
	a = 'b';
	while
		(a <= 'z') {
			putchar(a);
			a++;
		}
	putchar('\n');
	return (0);
}
